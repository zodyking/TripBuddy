import fs from 'fs';
import path from 'path';
import { fileURLToPath } from 'url';

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const rootDir = path.resolve(__dirname, '..');
const soundsDir = path.join(rootDir, 'public', 'sounds');

// Minimal valid MP3 frame - creates a very short (near-silent) placeholder
// Replace with actual notification sounds for production
const minimalMp3 = Buffer.from([
  0xFF, 0xFB, 0x90, 0x00, 0x00, 0x00, 0x00, 0x00,
  0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
  0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
  0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
  0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
  0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
  0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
  0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
  0xFF, 0xFB, 0x90, 0x00, 0x00, 0x00, 0x00, 0x00,
  0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
]);

const files = ['tractor-change.mp3', 'driver-change.mp3', 'success.mp3', 'error.mp3', 'reconnect.mp3'];

if (!fs.existsSync(soundsDir)) {
  fs.mkdirSync(soundsDir, { recursive: true });
}

files.forEach(file => {
  const filePath = path.join(soundsDir, file);
  if (!fs.existsSync(filePath)) {
    fs.writeFileSync(filePath, minimalMp3);
    console.log('Created:', file);
  } else {
    console.log('Exists:', file);
  }
});

console.log('Done - placeholder sounds created');
console.log('Note: Replace these with actual notification sounds for production');
